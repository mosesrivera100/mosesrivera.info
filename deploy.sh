# Render the Quarto website
quarto render

# Add all changes to Git
git add .

# Commit changes with a message (you can modify the message)
git commit -m "Render and update site"

# Push the changes to GitHub
git push
